Reference: 3-D Secure 2.x (Stripe + Adyen + Braintree)
Source: https://docs.stripe.com/3d-secure
Date verified: 2026-07-30

- methodUrl handler must return quickly; never block on issuer challenge.
- app return handler must call confirm with the same client_secret used in the original PaymentIntent.
- reject old client_secret values; expire after 30 minutes.
- timeout: if 3DS does not complete, render a structured error and start a fresh intent.

High-risk indicators:
- payment_method_options[card][request_three_d_secure]=any when no risk justification exists.
- completion flow sets the next intent's client_secret = prior failed intent's id without scoping.
